# sign_request and verify_request: the high-level API and the whole of @2hwvpm42. NOT built — py/ currently has the base builder and the key lens only. Must include the NEGATIVE requirement and its positive oracle: signing a request that carries a body with no content-digest in the covered set RAISES UncoveredBody, and verification recomputes the digest over the received body and raises DigestMismatch rather than trusting the covered header. A negative requirement produces no failing test by omission (cc ledger #22), so each of those needs a test that supplies the bad input and asserts the refusal, or it is not built. DEFAULT_COVERED is defined in base.py and nothing consumes it yet
kind: todo
created: 2026-09-03T23:48Z
closed: 2026-09-03T23:59Z

