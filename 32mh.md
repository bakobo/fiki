# py/tests/test_vectors.py::test_the_vectors_are_where_every_port_can_reach_them asserts VECTORS.parent.name == 'fiki', so it fails in any git worktree not named fiki (for example .worktrees/keri-profile), even though the vectors are exactly where they belong. CI passes only because the checkout directory happens to be named fiki. Assert on the relationship instead (VECTORS == repo root / 'vectors', with the root found from the test file's location, or git rev-parse --show-toplevel), not on a directory name.
kind: debt
tags: py
created: 2026-09-24T11:31Z

