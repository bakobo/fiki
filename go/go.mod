// fiki, in Go. Zero dependencies: the standard library carries ed25519 and sha256, and the
// RFC 8941 subset is hand-rolled for the reason every port hand-rolls it (`this.i` @2q9gv70t).
module github.com/bakobo/fiki/go

go 1.22
