# Freshness and replay: verify_request never looks at created, so a captured fiki-signed request replays forever. This is the SAME finding the keripy issue makes about keria never freshness-checking Signify-Timestamp, and fiki inherits it by having declined to decide. Verdict's docstring says so honestly ('replay is the caller's problem') which is better than implying otherwise, but a cron client and the service it calls will both assume the other side handled it. Decide: does fiki offer max_age= on verify (needs a clock, see ~3qdj, same design change), or does it stay pure and say in the README where replay protection has to live instead? Note the two ticks share one decision about whether fiki may read a clock at all
kind: idea
created: 2026-09-04T01:33Z
closed: 2026-09-04T15:14Z

