# heti must map fiki's three new error classes before it bumps its fiki pin: DuplicateComponent, InsufficientCoverage and UnknownKey (fiki commit 38df6b1, this.i @7f28p7xk, @6g9zjsv9). heti's boundary test walks FikiError.__subclasses__() and fails on any unmapped class (fiki @8zw78n0v), so the failure will be loud; this tick exists so the cause is findable. Suggested homes: DuplicateComponent with heti's malformed-input codes, InsufficientCoverage with its policy (e.rule.*) branch since the signature may be valid, UnknownKey with its unknown-signer/proof codes. Also note for heti and heti-wallet: fiki no longer uppercases @method (@22g0xkr8), so a lowercase method must be sent in the case it was signed in; fiki also now refuses unknown signature parameters and request-derived components without req in a response.
kind: todo
tags: heti
created: 2026-09-24T11:31Z

- 2026-09-24T12:01Z Commit 6e3f6fb adds two more classes heti must map: Unauthenticated (an unsigned 401 answering a request; policy/auth branch) and UnsupportedSigner (raised by a resolver when a key state has no single effective signer). fiki also now raises MissingKey earlier, while Signature-Input is read.
