# CONTRIBUTING.md:49-53 says every error fiki raises carries a stable symbolic code like e.state.conflict.r, but fiki deliberately carries none: this.i @8zw78n0v discriminates by class name and leaves heti to map classes onto e.* codes at its boundary. vectors/keri/ (@8vwrexxc) now adds a third vocabulary, the KERI profile's kebab-case section 9 codes (missing-signature, insufficient-coverage, ...), kept as the CODES table in vectors/keri/generate.py. The public contributor doc therefore promises something the code does not do and does not mention the vocabulary the KERI vectors use. Decide whether CONTRIBUTING.md should describe the class-plus-mapping reality (and name the profile codes as a vector-level vocabulary), or whether fiki's classes should carry a code attribute after all; the second reopens @8zw78n0v. Also docs/user-guide.md 'Handling errors' says the error names are identical across all five languages because the vectors pin them, which stopped being true when fiki-py gained DuplicateComponent, InsufficientCoverage and UnknownKey in commit 38df6b1.
kind: debt
tags: errors
created: 2026-09-24T11:31Z

